data: 
  cols_to_drop: [ 'mental_health_interview','Timestamp']
  y_column: 'treatment'
  num_features: 15

base:
  random_seed: 42

data_split:
  test_size: 0.3

train:
  max_depth: 9
  n_estimators: 9
  learning_rate": 0.1

evaluate:
  conf_matrix: "yes"